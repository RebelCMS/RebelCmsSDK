<?xml version="1.0"?>
<configuration>
    <appSettings>
        <add key="Database" value="${database.name}"/>
        <add key="User" value="${database.user}"/>
        <add key="Password" value="${database.password}"/>
    </appSettings>
</configuration>